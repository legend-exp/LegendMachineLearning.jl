# Variable-length set operations — padding, masking, batching
#
# Used by: HierarchicalSetModel (triggers per SiPM, variable count),
#          FlatSetTransformer (all triggers per event, variable count)
#
# TODO: Implement when adding trigger-level models.

# Placeholder — implementations will be added for trigger-level models
